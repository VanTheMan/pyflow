apiVersion: v1
kind: Pod
metadata:
  creationTimestamp: null
  labels:
    run: step1
  name: step1
  namespace: argo
spec:
  containers:
  - image: docker.io/library/step1:latest
    name: step1
    command: ["python", "-c"]
    args: ["from pyflow.pyflow import Pyflow; Pyflow().load_fn('step1')(1, 2, ); "]
    resources: {}
    env:
    - name: EXECUTION_ID
      value: "1697398434478445000"
    volumeMounts:
      - mountPath: /root/.pyflow
        name: pyflow
    imagePullPolicy: "Never"
  volumes:
    - name: pyflow
      hostPath:
        path: /.pyflow
        type: Directory
  restartPolicy: Never
status: {}